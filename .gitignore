# Dependencies
node_modules/
package-lock.json

# Temp / working directories (use .tmp/ only — no scattered temp dirs)
.tmp/
.codenomad/
test-results/
test-screenshots/
.test-screenshots/
.test-scripts/

# Playwright artifacts
playwright-report/
test-results/
*.png
!docs/**/*.png

# Server logs
server-stdout.txt
server-stderr.txt

# OS files
.DS_Store
Thumbs.db

# Auth secrets (template committed with empty values — local edits are gitignored)
auth.json

# Provider API keys (template committed with empty values — local edits are gitignored)
usage.json

# OpenCode config dashboard — local favorites store
opencode-dash-config.json
